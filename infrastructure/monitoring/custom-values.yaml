prometheus:
  prometheusSpec:
    resources:
      requests:
        memory: "256Mi"
      limits:
        memory: "512Mi"
grafana:
  resources:
    requests:
      memory: "128Mi"
    limits:
      memory: "512Mi"
